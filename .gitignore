.julia
*.jl.*.cov
*.jl.cov
*.jl.mem
*Manifest.toml
docs/build/*
*.ipynb_checkpoints*
*.md_checkpoints
*.ipynb.md
docs/src/generated
data
.snakemake
notebooks/scripts/kmer-saturation*
*.ipynb_checkpoints/
*.zip
*/kmer-saturation/*
*.gz
*.fastq
*.fq
*.fna
*.faa
*.fasta
*.fa
*tmp/*
*snakemake/logs/*
*.bam
*.sam
projects/me-cfs/reference_data/
*.out
*.err
projects/me-cfs/results/
projects/viral-exposome-discovery/results/
projects/ME_CFS/*
deps/build.log
*.jpg
*Project.toml
*.ipynb
test/test-development.jl
*.mmi
*.arrow
Manifest.toml
!docs/Manifest.toml
scratch*
build/*
package-lock.json
busco_downloads/*
busco_results/*
test/busco_downloads/*
test/busco_results/*
package.json
*DS_Store*
test/metadata/comebin_test_data*
node_modules/*
hpc-ci/coverage
hpc-ci/logs
hpc-ci/hpc-results.json

# Track A baseline benchmark: ignore bulky regenerable artifacts (downloaded
# references, simulated reads, per-cell contigs/QUAST/checkpoints). The small
# knowledge tier (track_a_results.tsv, power_analysis_*, artifacts/) is committed.
benchmarking/results/track_a_baseline/refs/
benchmarking/results/track_a_baseline/cells/

# td-jt7r indel benchmark working outputs (frontier runtime calibration,
# fixed-toy proof, smoke runs, and later sub-tasks) are regenerable by
# re-running the scripts, so the live output directories are not tracked.
#
# Curated evidence snapshots ARE committed. The negation below is what keeps
# them visible, and it is deliberately NOT left to the hyphen-vs-underscore
# naming convention: `git status` reporting a clean tree is indistinguishable
# from evidence being correctly tracked, so a mis-named evidence directory would
# vanish silently with no test able to fail. Any directory under
# benchmarking/results/ whose name contains "evidence" is re-included regardless
# of separator. This works because the parent benchmarking/results/ is not
# itself excluded and a later pattern wins.
benchmarking/results/td-jt7r-*/
!benchmarking/results/*evidence*/

# Agent worktrees
trees/

# Ephemeral AI handoff files
.handoff/

# Codex runtime artifacts (not portable; skills/ is tracked)
.codex/auth.json
.codex/config.toml
.codex/history.jsonl
.codex/models_cache.json
.codex/cloud-requirements-cache.json
.codex/log/
.codex/policy/
.codex/sessions/
.codex/tmp/
.codex/version.json
.codex/skills/.system
.codex/.codex-global-state.json
.codex/.personality_migration
.codex/sqlite/
.codex/vendor_imports/
.codex/shell_snapshots/*
.codex/archived_sessions/
.codex/worktrees/
.codex/mirror-manifest.json
.codex/mirror-report.md
.codex/mirror-dirty
.codex/prompts/MIGRATION_STATUS.md
.codex/state_*.sqlite*

.worktrees/

# Agent tool symlinks / worktrees / backups (td-vu6g4, 2026-06-22)
.claude/skills/
